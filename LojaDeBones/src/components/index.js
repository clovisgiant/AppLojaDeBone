import React from "react";
import { View, StyleSheet, Text } from "react-native";

export default function Index() {
    return <View>
        <Text>
            Loja de Bones e copo stanley Boça Line shop.
        </Text>
    </View>
};

const style = StyleSheet.create({
    container: {
        backgroundColor: '#DDD',
        flex: 1
    }
});
